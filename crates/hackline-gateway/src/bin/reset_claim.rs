//! `hackline-gateway reset-claim` — wipe the `users` table and seed
//! a new pending claim. Operator-on-the-VPS recovery; existing tokens
//! stop working immediately.

fn main() {
    unimplemented!("hackline-gateway reset-claim: scaffolding only");
}
