//! `hackline-gateway print-claim` — print the pending claim token.
//! Succeeds only while a claim is pending; lets an operator who
//! missed the boot log recover without a full reset.

fn main() {
    unimplemented!("hackline-gateway print-claim: scaffolding only");
}
